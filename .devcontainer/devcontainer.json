{
	"name": "rss-bridge dev",
	"build": { "dockerfile": "Dockerfile" },
	"forwardPorts": [3100, 9000],
	"postCreateCommand": "cp .devcontainer/nginx.conf /etc/nginx/conf.d/default.conf && cp docker-entrypoint.sh docker-entrypoint.sh && cp .devcontainer/xdebug.ini /usr/local/etc/php/conf.d/xdebug.ini && chmod a+x \"$(pwd)\" && rm -rf /var/www/html && ln -s \"$(pwd)\" /var/www/html"
}
